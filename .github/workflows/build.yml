name: Build Debian 13 GNOME ISO (Max Space)
on: [push, workflow_dispatch]

jobs:
  build-iso:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 步骤 1：暴力释放磁盘空间 (针对 GNOME 编译优化)
      - name: Maximize Build Disk Space
        uses: easimon/maximize-build-space@v10
        with:
          # 2026 年标准：将 root 预留降至最低，腾出近 30GB 可用空间
          root-reserve-mb: 1024
          swap-size-mb: 512
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'
          # 核心改动：开启 LVM 过载，这能压榨出更多空间
          overprovision-lvm: 'true'

      # 步骤 2：手动深度清淤 (解决 apt update 空间不足)
      - name: Brutal Disk Cleanup
        run: |
          echo "Before cleanup:"
          df -h
          # 删除不需要的巨型工具目录和语言包
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc "/usr/local/share/boost" "$AGENT_TOOLSDIRECTORY"
          sudo rm -rf /usr/share/man /usr/share/doc /usr/share/locale
          # 清理 apt 缓存
          sudo apt-get clean
          echo "After cleanup:"
          df -h

      # 步骤 3：安装工具 (现在空间足够了)
      - name: Install Live Build Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y live-build debootstrap devscripts

      # 步骤 4：配置 Live Build (确保驱动和 GNOME)
      - name: Configure Live Build
        run: |
          mkdir -p live-build-dir && cd live-build-dir
          
          # 初始化配置 (指定 2026 年当下的 Trixie 稳定版)
          lb config \
            --distribution trixie \
            --debian-installer live \
            --archive-areas "main contrib non-free non-free-firmware" \
            --apt-indices false \
            --memtest none \
            --mirror-bootstrap "http://deb.debian.org/debian/"

          # 注入软件包列表 (确保安装 GNOME 和计算棒驱动)
          mkdir -p config/package-lists
          cat <<EOF > config/package-lists/my-packages.list.chroot
          task-gnome-desktop
          locales
          fonts-wqy-microhei
          network-manager-gnome
          python3
          python3-pip
          python3-venv
          supervisor
          firmware-linux
          firmware-realtek
          firmware-intel-sound
          intel-media-va-driver-non-free
          firefox-esr
          sudo
          nano
          htop
          zram-tools
          EOF

          # 预设语言为中文
          mkdir -p config/includes.chroot/etc/default
          echo "LANG=zh_CN.UTF-8" > config/includes.chroot/etc/default/locale

      # 步骤 5：编译 ISO (漫长的等待)
      - name: Build ISO
        run: |
          cd live-build-dir
          # live-build 自动处理 2026 年的密钥验证
          sudo lb build

      # 步骤 6：定位并上传
      - name: Locate and Rename ISO
        run: |
          find . -name "*.hybrid.iso" -exec mv {} ./debian13-gnome-stick.iso \;
          find . -name "*.iso" ! -name "debian13-gnome-stick.iso" -exec mv {} ./debian13-gnome-stick.iso \;

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v4
        with:
          name: debian13-gnome-iso
          path: debian13-gnome-stick.iso
